/* Store the radius of a circle in a variable and calculate the area. */

using System;

public class Circle
{
    public void Input(double r, double pi)

    {

        double area = pi*r*r;
        Console.WriteLine("The area of circle is " + area );
        
    }
    
}
